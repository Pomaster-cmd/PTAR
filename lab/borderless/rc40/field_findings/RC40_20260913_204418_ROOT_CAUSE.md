# RC40 field finding — 2026-09-13 20:44 (Win8.1 / GTX 960M)

RC40 is a hardware FAIL for the intended native-composition framing goal.

## Exact active payload

- runtime SHA256: `774f88c976ed296d7496d5fce1fad9f8e1056337ee70e0111371866074e75669`
- sidecar SHA256: `ce497e72837f95503f877f73239c63a692c90b8646dbf7a3bbd4ba8082083416`

## Decisive evidence

The sidecar log proves RC40 actually acquired the window authority that RC38/RC39 failed to prove:

- `AUTO_START_RC40_POSTWNDPROC geometry 1280 720 1920 1080`
- `ATTACH_RC40_POSTWNDPROC_PENDING 1280 720 1920 1080`
- `IAT_HOOKS 3 0 0 0`
- `ACTIVE_RC40_POSTWNDPROC ...` decodes to render 1280x720 and presenter 1920x1080.

The production log simultaneously proves P1U46 already has:

- game-controlled backbuffer 1280x720;
- native presenter 1920x1080;
- aspect-fit rect 1920x1080 at offset 0,0;
- direct sampling of the real game backbuffer;
- logical game presentation mode 1280x720;
- viewport/scissor hooks intentionally not installed.

Therefore the residual visual mismatch is not a Win32 borderless/window-authority problem. RC40 falsifies that hypothesis.

## Architectural conclusion

P1U46 currently conflates logical composition geometry with the reduced physical render surface: the game is allowed to compose at 1280x720, then the completed frame is scaled to 1920x1080. Window authority cannot recover native composition/FOV/UI semantics after composition has already happened at the reduced logical geometry.

The next candidate must return to a split logical/physical raster contract:

1. preserve native logical composition/presentation geometry (1920x1080 for the current field target);
2. keep the true render/backbuffer surface at 1280x720;
3. selectively map viewport/scissor coordinates from logical space into physical render space;
4. classify the swapchain and canonical offscreen render family instead of globally scaling every raster call;
5. leave native window/input geometry untouched.

Historical P1U16/P1U22/P1U26 field evidence shows this exact family of mechanisms existed: separate logical and true backbuffer sizes, context slot 44 `RSSetViewports`, slot 45 `RSSetScissorRects`, and P1U22 cached RTV/DSV offscreen classification.

No further RC38/RC39/RC40 window-authority variant should be field-tested without a new logical/native-to-subraster framing gate.
